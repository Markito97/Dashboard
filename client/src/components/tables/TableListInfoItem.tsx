import { FC } from "react";

interface ITableListInfoItem {
  time: string;
  persons: number;
}

export const TableListInfoItem: FC<ITableListInfoItem> = ({
  time,
  persons,
}) => {
  return null;
};
